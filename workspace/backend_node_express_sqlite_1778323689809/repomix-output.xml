This file is a merged representation of the entire codebase, combined into a single document by Repomix.

<file_summary>
This section contains a summary of this file.

<purpose>
This file contains a packed representation of the entire repository's contents.
It is designed to be easily consumable by AI systems for analysis, code review,
or other automated processes.
</purpose>

<file_format>
The content is organized as follows:
1. This summary section
2. Repository information
3. Directory structure
4. Repository files (if enabled)
5. Multiple file entries, each consisting of:
  - File path as an attribute
  - Full contents of the file
</file_format>

<usage_guidelines>
- This file should be treated as read-only. Any changes should be made to the
  original repository files, not this packed version.
- When processing this file, use the file path to distinguish
  between different files in the repository.
- Be aware that this file may contain sensitive information. Handle it with
  the same level of security as you would the original repository.
</usage_guidelines>

<notes>
- Some files may have been excluded based on .gitignore rules and Repomix's configuration
- Binary files are not included in this packed representation. Please refer to the Repository Structure section for a complete list of file paths, including binary files
- Files matching patterns in .gitignore are excluded
- Files matching default ignore patterns are excluded
- Files are sorted by Git change count (files with more changes are at the bottom)
</notes>

</file_summary>

<directory_structure>
contract.json
server.js
</directory_structure>

<files>
This section contains the contents of the repository's files.

<file path="contract.json">
{"id":"acacde35-9e63-4fb2-a5cd-02f165435925","context":"Backend_Node_Express_SQLite","description":"Genera il codice backend (Node.js/Express/SQLite) per gestire la Wave 2 del Gestionale BIRROTECA.\nEndpoints richiesti:\n- POST /prenotazioni (crea prenotazione con nome_cliente, numero_persone, data_ora, tavolo_id)\n- DELETE /prenotazioni/:id (cancella prenotazione esistente)\n- GET /prenotazioni/mie (lista prenotazioni)\nDatabase Schema (SQLite):\n- table 'tavoli' (id INTEGER PRIMARY KEY, numero_posti INTEGER)\n- table 'prenotazioni' (id INTEGER PRIMARY KEY, nome_cliente TEXT, numero_persone INTEGER, data_ora TEXT, tavolo_id INTEGER, stato TEXT DEFAULT 'attiva')\nRispetta rigorosamente i vincoli A2A-OCL indicati.","a2a_ocl_constraints":["context Prenotazione inv: self.numero_persone <= self.tavolo.numero_posti","context Prenotazione inv: self.tavolo.prenotazioni->forAll(p | p.data_ora != self.data_ora or p.id = self.id)"],"metadata":{}}
</file>

<file path="server.js">
const express = require('express');
const sqlite3 = require('sqlite3').verbose();

const app = express();
app.use(express.json());

const db = new sqlite3.Database(':memory:');

// Create tables
const createTables = () => {
  db.run(`CREATE TABLE tavoli (id INTEGER PRIMARY KEY, numero_posti INTEGER);`);
  db.run(`CREATE TABLE prenotazioni (
    id INTEGER PRIMARY KEY,
    nome_cliente TEXT,
    numero_persone INTEGER,
    data_ora TEXT,
    tavolo_id INTEGER,
    stato TEXT DEFAULT 'attiva',
    FOREIGN KEY(tavolo_id) REFERENCES tavoli(id)
  );`);
};

createTables();

// POST /prenotazioni
app.post('/prenotazioni', (req, res) => {
  const { nome_cliente, numero_persone, data_ora, tavolo_id } = req.body;

  // Check table capacity
  db.get('SELECT numero_posti FROM tavoli WHERE id = ?', [tavolo_id], (err, row) => {
    if (err) {
      return res.status(500).send({ error: 'Errore database' });
    }
    if (!row || numero_persone > row.numero_posti) {
      return res.status(400).send({ error: 'Numero persone eccede la capacità del tavolo' });
    }

    // Check for existing booking at the same time
    db.get('SELECT * FROM prenotazioni WHERE tavolo_id = ? AND data_ora = ?', [tavolo_id, data_ora], (err, existing) => {
      if (err) {
        return res.status(500).send({ error: 'Errore database' });
      }
      if (existing) {
        return res.status(400).send({ error: 'Prenotazione esistente nel medesimo orario' });
      }

      // Create new booking
      db.run('INSERT INTO prenotazioni (nome_cliente, numero_persone, data_ora, tavolo_id) VALUES (?, ?, ?, ?)',
        [nome_cliente, numero_persone, data_ora, tavolo_id],
        function (err) {
          if (err) {
            return res.status(500).send({ error: 'Errore database' });
          }
          res.status(201).send({ id: this.lastID });
        });
    });
  });
});

// DELETE /prenotazioni/:id
app.delete('/prenotazioni/:id', (req, res) => {
  const { id } = req.params;
  db.run('DELETE FROM prenotazioni WHERE id = ?', [id], function (err) {
    if (err) {
      return res.status(500).send({ error: 'Errore database' });
    }
    if (this.changes === 0) {
      return res.status(404).send({ error: 'Prenotazione non trovata' });
    }
    res.status(204).send();
  });
});

// GET /prenotazioni/mie
app.get('/prenotazioni/mie', (req, res) => {
  db.all('SELECT * FROM prenotazioni', [], (err, rows) => {
    if (err) {
      return res.status(500).send({ error: 'Errore database' });
    }
    res.status(200).send(rows);
  });
});

const PORT = process.env.PORT || 3000;
app.listen(PORT, () => {
  console.log(`Server is running on port ${PORT}`);
});
</file>

</files>
